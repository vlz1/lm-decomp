#ifndef JORREFLEXIBLE_HPP
#define JORREFLEXIBLE_HPP

// This is just a stub that's referenced in the RTTI of other classes.
// It doesn't seem to have a vtable or any members.
class JORReflexible {
};

#endif
